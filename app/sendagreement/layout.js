import React from 'react';
import '../globals.css';

const SendAgreement = ({ children }) => {
    return (
      <div>
        <nav>
          {/* Add navigation or other layout elements */}
        </nav>
        <main>{children}</main>
      </div>
    );
  };
  
  export default SendAgreement;
