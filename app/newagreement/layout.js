import React from 'react';
import '../globals.css';

const NewAgreementLayout = ({ children }) => {
  return (
    <div>
      <nav>
        {/* Add navigation or other layout elements */}
      </nav>
      <main>{children}</main>
    </div>
  );
};

export default NewAgreementLayout;
